<%# product creation errors here, @product coming from the create action %>



<%# rails guide form_helper %>
<%# 2.3.1 Dealing with Namespaces
If you have created namespaced routes, form_with has a nifty shorthand for that too. If your application has an admin namespace then %>
<h5 class=" red-text">If Category is not dropping just relaod the page</h5>
<%# <%= form_with(model: [:user, product], local: true) do |f|%> 
  
    
      <h4>Product Name:</h4> 
      <%= f.text_field :name  %><br/>

      <h4>Product Price:</h4> 
      <%= f.number_field :price, value: product.price ? '%.2f' % product.price : nil, min: 0, step: 0.01 %>$<br/>

      <h4>Product Description:</h4> 
      <%= f.text_field :description  %><br/>
<br/>
    <h4>Product Image (recommended)</h4> 
    <%= f.file_field :image  %><br/>
<br>

 <h4>Category</h4> 
 <%# <form_for (product.product_categories)> %>
    <%= f.fields_for :product_categories do |product_category|%>
        <%=render 'product_category_fields', product_category: product_category%>
        
    <%end%>
  
<%# --------------------------------------------------- %>
<%# this is showing when i inspect but not displaying for some reason bug!!! %>


    <button class="btn-floating btn-large pulse pink" type="submit" name="action">Create 
    <i class="material-icons left">cloud</i>
  </button>

  
<br/>
<br/>
<%# -------------------------------------------------- %>


<%# back %>
<br><br>
 <a class="btn waves-effect waves-light" href='<%=root_path%>'>Back
  </a>
